# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool: default

steps:
- task: CmdLine@2
  inputs:
    script: 'unix2dos bats/*.*'

- task: Bash@3
  inputs:
    filePath: 'bats/VM-setup.sh'
    
